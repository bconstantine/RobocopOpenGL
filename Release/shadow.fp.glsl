#version 430

void main()
{
    //leave it empty, because shadow shader automatically set a fragment depth from the z coordinate
    // it is as if this process is done
    //gl_FragDepth = gl_FragCoord.z;
}  